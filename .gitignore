# secrets
.env
.env.*

# API cache. The raw balance rows are ~660 MB and never belong in git; the
# compact cache below is what the daily job carries forward between runs.
data/*
!data/cache.json.gz
!data/mcap_deep.json
!data/bubbles80.json
!data/persistence.json

# python
__pycache__/
*.pyc

# generated site (rebuild with: python3 make.py --build)
.DS_Store
Claude outputs/
